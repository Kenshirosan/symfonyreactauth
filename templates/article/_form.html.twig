{{ form_start(form, {attr: {class: 'form mt-3 mb-3'}}) }}
{#    {{ form_widget(form) }}#}
<div class="form-group">
    {{ form_row(form.title, {attr: {placeholder: 'The title', class: 'form-control form-control-lg'},label_attr: {class: 'form-label'}}) }}
</div>
<div class="form-group">
    {{ form_row(form.body, {attr: {placeholder: 'The body', class: 'form-control form-control-lg'},label_attr: {class: 'form-label'}}) }}
</div>
<div class="form-group">
    {{ form_row(form.user_id, {attr: {class: 'form-select'}}) }}
</div>
<div class="form-group mt-3 mb-3">
    <input type="submit" class="btn btn-primary p-3" value="{{ button_label|default('Créer') }}" />
</div>
{{ form_end(form) }}
